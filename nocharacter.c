#include<stdio.h>
#include<conio.h>
void main()
{
char a[100],count,i;
printf("\nEnter a string");
scanf("%s",&a[i]);
for(i=0;a[i]!=NULL;i++)
{
count++;
}
printf("\n number of characters %d",count);
}
